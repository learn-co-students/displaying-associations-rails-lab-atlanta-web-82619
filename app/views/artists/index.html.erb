<h1>Artist List:</h1>

<ul>
    <% @artists.each do |artist| %>
        <li><p><%= link_to artist.name, artist_path(artist) %></p>
            <p><%= pluralize(artist.song_count, 'song') %>
        </li>
        
    <% end %>
</ul>